name: Build Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

concurrency:
  group: release-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  create-release:
    name: Create Release Draft
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Create shared release draft
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release view "${GITHUB_REF_NAME}" --repo "${GITHUB_REPOSITORY}" >/dev/null 2>&1 || \
            gh release create "${GITHUB_REF_NAME}" --repo "${GITHUB_REPOSITORY}" --draft --title "AlipanBuddy ${GITHUB_REF_NAME}"

  release:
    name: Build Release
    needs: [create-release]
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            args: --target x86_64-pc-windows-msvc --bundles nsis
          - os: windows-latest
            target: aarch64-pc-windows-msvc
            args: --target aarch64-pc-windows-msvc --bundles nsis
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            args: --target x86_64-unknown-linux-gnu
          - os: ubuntu-22.04-arm
            target: aarch64-unknown-linux-gnu
            args: --target aarch64-unknown-linux-gnu
    runs-on: ${{ matrix.os }}

    permissions:
      contents: write

    steps:
      - name: Checkout Git repository
        uses: actions/checkout@v4

      # pnpm has to exist before setup-node so that `cache: pnpm` can find the store.
      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          run_install: false

      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: 22.12.0
          cache: 'pnpm'

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Rust build
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target

      - name: Install Linux dependencies
        if: startsWith(matrix.os, 'ubuntu')
        run: |
          sudo apt-get update
          sudo apt-get install -y --fix-missing libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libssl-dev rpm

      - name: Install Dependencies
        run: |
          pnpm install --frozen-lockfile

      - name: Generate Release Secrets
        env:
          ALIYUN_APP_ID: ${{ secrets.ALIYUN_APP_ID }}
          ALIYUN_APP_SECRET: ${{ secrets.ALIYUN_APP_SECRET }}
        run: |
          node scripts/generate-secrets.mjs --mode=ci

      - name: Prepare aria2c sidecar
        run: |
          node scripts/prepare-sidecars.mjs ${{ matrix.target }}

      - name: Build Tauri App (Windows installers)
        if: startsWith(matrix.os, 'windows')
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: AlipanBuddy ${{ github.ref_name }}
          releaseDraft: true
          args: ${{ matrix.args }}

      # Linux skips Tauri's bundler: scripts/build-linux-packages.sh assembles the
      # deb / rpm from one FHS staging tree (binaries in /usr/lib/alipanbuddy with a
      # /usr/bin symlink, so the bundled aria2c never collides with the system one).
      - name: Build Tauri App (Linux binary)
        if: startsWith(matrix.os, 'ubuntu')
        run: pnpm exec tauri build ${{ matrix.args }} --no-bundle

      - name: Build Linux packages
        if: startsWith(matrix.os, 'ubuntu')
        run: bash scripts/build-linux-packages.sh ${{ matrix.target }} --require-all

      - name: Upload Linux packages to release
        if: startsWith(matrix.os, 'ubuntu')
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "${GITHUB_REF_NAME}" dist-linux/*.tar.gz dist-linux/*.deb dist-linux/*.rpm --repo "${GITHUB_REPOSITORY}" --clobber

  finalize-release:
    name: Finalize Release
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Git repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate GitHub Release Notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          release_doc="docs/releases/${GITHUB_REF_NAME}.md"
          if [[ -f "${release_doc}" ]]; then
            echo "Using release notes from ${release_doc}"
            cp "${release_doc}" release-notes.md
          else
            echo "No versioned release document found; generating GitHub notes"
            gh api "repos/${GITHUB_REPOSITORY}/releases/generate-notes" \
              -f tag_name="${GITHUB_REF_NAME}" \
              --jq .body > release-notes.md
          fi
          if [[ ! -s release-notes.md ]]; then
            previous_tag="$(git tag --sort=-version:refname | grep -Fxv "${GITHUB_REF_NAME}" | head -n 1 || true)"
            printf '## 更新内容\n\n' > release-notes.md
            if [[ -n "${previous_tag}" ]]; then
              git log --format="- %s (%h)" "${previous_tag}..${GITHUB_SHA}" >> release-notes.md
            else
              git log --format="- %s (%h)" -1 "${GITHUB_SHA}" >> release-notes.md
            fi
          fi
          if [[ ! -s release-notes.md ]]; then
            echo 'Release notes are empty; refusing to publish' >&2
            exit 1
          fi
          gh release edit "${GITHUB_REF_NAME}" \
            --repo "${GITHUB_REPOSITORY}" \
            --title "AlipanBuddy ${GITHUB_REF_NAME}" \
            --notes-file release-notes.md \
            --prerelease=false \
            --draft=false
